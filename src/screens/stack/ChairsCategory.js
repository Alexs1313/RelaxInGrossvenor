import {StyleSheet, Text, View} from 'react-native';

const ChairsCategory = () => {
  return (
    <View style={styles.container}>
      <Text>ChairsCategory</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
});

export default ChairsCategory;
